plugins {
    id 'java'
}

group = 'org.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.testng:testng:7.11.0'
    testImplementation 'org.junit.jupiter:junit-jupiter-engine:5.10.0' // Use the latest stable version

}

test {
    useJUnitPlatform()
}